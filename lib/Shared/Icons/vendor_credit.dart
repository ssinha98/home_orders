import 'package:flutter/material.dart';

class VendorCreditIcon extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
        child: Image.asset(
      "assets/Vendor_credit.png",
      scale: 0.7,
    ));
  }
}
